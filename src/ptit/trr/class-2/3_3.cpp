#include <iostream>
using namespace std;

void find_combination() {
}

void findMatrix(const int level) {
    auto matrix = new int[level][level]{0};
    int max = level * level;

    // int combinationCases =
}


int main() {
    int test = 2;
    const int n[test] = {3, 4};

    while (test--) {
        findMatrix(n[test]);
    }
}
